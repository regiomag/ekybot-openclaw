/**
 * ⚠️  DEPRECATED — Ne pas utiliser ce fichier.
 *
 * La vraie implémentation est dans `src/lib/request-auth.ts`.
 * Le tsconfig mappe `@/*` → `src/*`, donc tous les imports `@/lib/request-auth`
 * pointent vers src/lib/request-auth.ts, PAS vers ce fichier.
 *
 * Ce fichier existe uniquement pour éviter les confusions futures.
 * Il ne doit pas être importé directement.
 */

export { resolveRequestAuth } from '../src/lib/request-auth';
